# Base44 dev defaults — placeholders so the app boots without credentials.
# Real secrets (Razorpay keys, SMTP, etc.) are delivered via /run/base44/app.env
# and always override these values.
SECRET_KEY=base44-dev-secret-key-change-me-9f3a7c2e1b8d4a6f
DATABASE_URL=sqlite:///shoplinq.db
FLASK_DEBUG=1
DEMO_MODE=1
SESSION_COOKIE_SECURE=0
